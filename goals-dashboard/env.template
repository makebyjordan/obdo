# Database Configuration
# Reemplaza con tus credenciales reales de PostgreSQL
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/goals_dashboard"

# NextAuth Configuration
# URL de tu aplicación (en producción usa tu dominio)
NEXTAUTH_URL="http://localhost:3000"

# Secret para NextAuth (genera uno único con: openssl rand -base64 32)
NEXTAUTH_SECRET="tu-secret-key-muy-segura-aqui-cambiala"

# Node Environment
NODE_ENV="production"

# Instrucciones:
# 1. Copia este archivo: cp env.template .env
# 2. Edita .env con tus valores reales
# 3. NUNCA subas el archivo .env a git
